const base = 'http://localhost:8080/';
export const Routes = {
  bien: {
    base: base + 'bien/',
    search: base + 'bien/research/',
  },
  user: {
    base: base + 'user/',
    register: base + 'user/',
    login: base + 'user/login/',
  },
  picture: {
    base: base + 'photo/',
  }
};
